import DataServicios from '../../Json/Services.json'

export const loadServices = async () => {
  return DataServicios
}

export const saveServices = async (payload) => {return payload}
export const updateServices= async (payload) => { return payload }